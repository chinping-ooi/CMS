namespace CMS.Enum;

public enum UserRoles
{
    Admin = 1,
    Customer = 2,
    Member = 3
}

public enum Status
{
    Active = 1,
    Inactive = 0
}

public enum TaskPriority
{
    Low = 1,
    Medium = 2,
    High = 3
}

public enum Record_Type
{
    New = 1,
    Amended = 3,
    Deleted = 5
}